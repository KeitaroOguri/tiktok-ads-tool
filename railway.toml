[build]
builder = "nixpacks"

[deploy]
startCommand = "python3 -m streamlit run auto_manager.py --server.port $PORT --server.address 0.0.0.0 --server.headless true --server.enableCORS false --server.enableXsrfProtection false"
restartPolicyType = "always"
